name: 构建 OpenWrt (自定义内核)

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH 连接至 Actions'
        required: false
        default: 'false'
        type: boolean
  schedule:
    - cron: 0 8 * * 2

env:
  REPO_URL: https://github.com/kkstone/immortalwrt-ipq50xx
  REPO_BRANCH: openwrt-21.02
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: .config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  UPLOAD_BIN_DIR: true
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: false
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: 检出代码
      uses: actions/checkout@main

    - name: 初始化编译环境
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        dpkg-query -Wf '${Installed-Size}\t${Package}\n' | sort -n | tail -n 100
        df -h
        echo "正在移除大体积软件包"
        sudo apt-get remove -y '^dotnet-.*'
        sudo apt-get remove -y '^llvm-.*'
        sudo apt-get remove -y 'php.*'
        sudo apt-get remove -y '^mongodb-.*'
        sudo apt-get remove -y '^mysql-.*'
        sudo apt-get remove -y azure-cli google-cloud-sdk google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri
        sudo apt-get autoremove -y
        sudo apt-get clean
        df -h
        echo "正在移除大体积目录"
        sudo rm -rf /usr/share/dotnet/
        sudo rm -rf /usr/local/graalvm/
        sudo rm -rf /usr/local/.ghcup/
        sudo rm -rf /usr/local/share/powershell
        sudo rm -rf /usr/local/share/chromium
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /usr/local/lib/node_modules
        df -h
        sudo -E apt-get -qq update
        sudo apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
          bzip2 ccache clang cmake cpio curl device-tree-compiler ecj fastjar flex gawk gettext gcc-multilib \
          g++-multilib git gnutls-dev gperf haveged help2man intltool lib32gcc-s1 libc6-dev-i386 libelf-dev \
          libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5 \
          libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lld llvm lrzsz mkisofs msmtp \
          nano ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pip python3-ply \
          python3-pyelftools qemu-utils re2c rsync scons squashfs-tools subversion swig \
          texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    - name: 克隆源代码
      working-directory: /workdir
      run: |
        df -hT $PWD
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    # ========== 新增：克隆自定义内核源码 ==========
    - name: 克隆自定义内核源码
      run: |
        cd /workdir
        echo "克隆内核源码: https://git.codelinaro.org/clo/qsdk/oss/kernel/linux-ipq-5.4.git"
        git clone https://git.codelinaro.org/clo/qsdk/oss/kernel/linux-ipq-5.4.git kernel-source
        cd kernel-source
        git checkout d5fcb18e5420670c8734c6a659873e73adab6dac
        echo "内核版本: $(git log -1 --format='%h %s')"
        cd ..
        
        # 打包内核源码
        echo "打包内核源码为 linux-5.4-qsdk-11.5.0.5.tar.xz"
        tar -cJf kernel-source.tar.xz kernel-source/
        
        # 创建 dl 目录并复制内核源码包
        mkdir -p openwrt/dl
        cp kernel-source.tar.xz openwrt/dl/linux-5.4-qsdk-11.5.0.5.tar.xz
        
        # 清理临时文件
        rm -rf kernel-source kernel-source.tar.xz
        
        echo "自定义内核源码已准备就绪: openwrt/dl/linux-5.4-qsdk-11.5.0.5.tar.xz"
    
    # ========== 新增：修改内核 Makefile ==========
    - name: 修改内核配置使用本地源码
      run: |
        cd openwrt
        TARGET_MAKEFILE="target/linux/ipq50xx/Makefile"
        
        # 备份原文件
        cp $TARGET_MAKEFILE $TARGET_MAKEFILE.bak
        
        # 注释掉 Git 仓库配置，强制使用本地 tar 包
        sed -i 's/^CONFIG_KERNEL_GIT_CLONE_URI/# CONFIG_KERNEL_GIT_CLONE_URI/' $TARGET_MAKEFILE
        sed -i 's/^CONFIG_KERNEL_GIT_REF/# CONFIG_KERNEL_GIT_REF/' $TARGET_MAKEFILE
        
        # 确保内核版本匹配
        sed -i 's/LINUX_VERSION-5.4:=.*/LINUX_VERSION-5.4:=-qsdk-11.5.0.5/' $TARGET_MAKEFILE
        
        echo "已修改内核配置，将使用本地源码包"
        echo "当前配置:"
        grep -E "LINUX_VERSION|GIT_CLONE|GIT_REF" $TARGET_MAKEFILE || true

    - name: 启用编译缓存
      uses: klever1988/cachewrtbuild@main
      with:
        ccache: 'true'
        mixkey: 'immortalwrt21-cortexa7-vfpv4-custom-kernel'
        prefix: ${{ github.workspace }}/openwrt

    - name: 加载自定义 feeds
      run: |
        [ -e $FEEDS_CONF ] && mv $FEEDS_CONF openwrt/feeds.conf.default
        chmod +x $DIY_P1_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P1_SH
   
    - name: 更新 feeds
      run: cd openwrt && ./scripts/feeds update -a

    - name: 修复 golang 版本
      run: |
        cd openwrt
        rm -rf feeds/packages/lang/golang
        git clone https://github.com/kenzok8/golang -b 1.25 feeds/packages/lang/golang

    - name: 安装 feeds
      run: cd openwrt && ./scripts/feeds install -a

    - name: 升级部分网络软件包版本
      run: |
        cd openwrt

        rm -rf feeds/packages/net/natmap
        wget -r --no-parent https://cdn.jsdelivr.net/gh/immortalwrt/packages@master/net/natmap/
        cp -rf cdn.jsdelivr.net/gh/immortalwrt/packages@master/net/natmap feeds/packages/net/natmap/
        rm -rf cdn.jsdelivr.net
        find feeds/packages/net/natmap -name index.html -exec rm {} \;

        rm -rf feeds/packages/net/ddns-go
        wget -r --no-parent https://cdn.jsdelivr.net/gh/immortalwrt/packages@master/net/ddns-go/
        cp -rf cdn.jsdelivr.net/gh/immortalwrt/packages@master/net/ddns-go feeds/packages/net/ddns-go/
        rm -rf cdn.jsdelivr.net
        find feeds/packages/net/ddns-go -name index.html -exec rm {} \;    

        rm -rf feeds/packages/net/frp
        wget -r --no-parent https://cdn.jsdelivr.net/gh/immortalwrt/packages@master/net/frp/
        cp -rf cdn.jsdelivr.net/gh/immortalwrt/packages@master/net/frp feeds/packages/net/frp/
        rm -rf cdn.jsdelivr.net
        find feeds/packages/net/frp -name index.html -exec rm {} \;

    - name: 加载自定义配置
      run: |
        [ -e files ] && mv files openwrt/files
        [ -e $CONFIG_FILE ] && mv $CONFIG_FILE openwrt/.config
        chmod +x $DIY_P2_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: SSH 连接至 Actions
      uses: P3TERX/ssh2actions@v1.0.0
      if: (github.event.inputs.ssh == 'true' && github.event.inputs.ssh  != 'false') || contains(github.event.action, 'ssh')
      env:
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}

    # ========== 修改：注释掉原有的内核下载，改为验证本地内核包 ==========
    - name: 验证内核源码包
      run: |
        cd openwrt
        echo "检查 dl 目录中的内核源码包:"
        ls -lh dl/linux-*.tar.xz || echo "警告: 未找到内核源码包"
        
        # 如果存在旧的内核下载步骤，这里不再需要
        # 原有的 wget 下载 backports 仍然保留（因为 backports Git 需要授权）
        if [ ! -f "dl/backports-20210222-5.4-qsdk-11.5.0.5.tar.xz" ]; then
          echo "下载 backports 从 Dropbox..."
          wget "https://www.dropbox.com/scl/fi/9ts30p2csnlb9imaf8k68/backports-20210222-5.4-qsdk-11.5.0.5.tar.xz?rlkey=sntbyfjgg86gu2uoocgb9ggp1&st=7w81hl56&dl=1" -O dl/backports-20210222-5.4-qsdk-11.5.0.5.tar.xz
        fi

    - name: 下载软件包
      id: package
      run: |
        cd openwrt
        make defconfig
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: 编译固件
      id: compile
      run: |
        cd openwrt
        echo -e "$(nproc) 线程编译"
        make -j$(nproc) || make -j1 || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV
        
    - name: 检查磁盘空间
      if: (!cancelled())
      run: df -hT

    - name: 上传 bin 目录
      uses: actions/upload-artifact@main
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      with:
        name: OpenWrt_bin${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: openwrt/bin

    - name: 整理固件文件
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: 上传固件目录
      uses: actions/upload-artifact@main
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE }}

    - name: 删除旧的工作流运行记录
      uses: Mattraks/delete-workflow-runs@main
      with:
        retain_days: 1
        keep_minimum_runs: 3
